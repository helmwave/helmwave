name: Gif

on:
  pull_request:
jobs:
  pr_check:
    runs-on: ubuntu-latest
    name: Congrats
    steps:
    - name: PR Action
      uses: koushikmohan1996/action-pr-gifs@V1.1
      with:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}